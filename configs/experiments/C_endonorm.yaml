defaults:
  - ../base

model:
  model_type: b2
  in_channels: 6
  pretrained_path: weights/mit_b2.pth

data:
  input_mode: rgb_norm
  preprocessed_root: data/preprocessed/kvasir-seg

training:
  loss: structure

logging:
  run_name: C_endonorm
