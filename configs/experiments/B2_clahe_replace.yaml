defaults:
  - ../base

model:
  model_type: b2
  in_channels: 3
  pretrained_path: weights/mit_b2.pth

data:
  input_mode: clahe
  preprocessed_root: data/preprocessed/kvasir-seg

training:
  loss: structure

logging:
  run_name: B2_clahe_replace
