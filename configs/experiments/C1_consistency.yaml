defaults:
  - ../base

augmentation:
  strong_style: true
  fourier: true

training:
  loss: structure
  consistency:
    enabled: true
    consistency_weight: 1.0

logging:
  run_name: C1_consistency
