# Upstream

Repositorio de origem, ambiente Conda e comando do ESFPNet.

```bash
make setup
make train MODEL=esfpnet
```

Modelo em `upstream/commands.yaml`.

ESFPNet usa o repo oficial em `upstream/vendor/esfpnet` com overlay CLI para o fluxo Kvasir.
